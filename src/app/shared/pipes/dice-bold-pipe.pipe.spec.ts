import { DiceBoldPipePipe } from './dice-bold-pipe.pipe';

describe('DiceBoldPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DiceBoldPipePipe();
    expect(pipe).toBeTruthy();
  });
});
